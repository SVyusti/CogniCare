{
    "$schema": "https://railway.app/railway.schema.json",
    "deploy": {
        "startCommand": "pip install -r requirements.txt && python -m langflow run && streamlit run \uD83C\uDFE0_Dashboard.py"
    }
}